meta {
  name: 적금 상세 불러오기
  type: http
  seq: 4
}

get {
  url: {{base}}/account/details?accountId=1
  body: none
  auth: inherit
}

params:query {
  accountId: 1
}

script:pre-request {
  const loginLib = require('./scripts/login');
  return loginLib.loginByCase(req, "chulsoo");
}

settings {
  encodeUrl: true
}
