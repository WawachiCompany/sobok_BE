files:
  "/etc/nginx/conf.d/00_redirect_http_to_https.conf":
    mode: "644"
    owner: root
    group: root
    content: |
      server {
          listen 80;
          server_name _;

          location / {
              return 301 https://$host$request_uri;
          }
      }